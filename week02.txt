week02
- linux command : ls, ls -a, ls -al, pwd, cd git, cd .., echo "blahblahblah" >> README.md
- git command
  git init
  git status //현재 상황//
  git add week01.txt //스테이지에 올리는 것//
  git config --global user.email "you@example.com"
  git config --global user.name "your name"
  git config --global --list
  git commit -m "v0"
  git log
  git checkout hashcode
  git checkout -
  git branch -M main
  git remote add origin https://github.com/inhatckimtaeganc/test.git
  git remote -v
  git push -u origin main